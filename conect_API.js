
export function returnAPI(message_user) {
    // ele vai pegar a mensagem do usuário, e enviar para api do OpenAI
    // devolver a resposta da api OpenAI e return como função
    return message_user;
}